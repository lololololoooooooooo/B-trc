{
  "name": "iot-battery-tracker",
  "version": "1.0.0",
  "description": "IoT Battery Tracker with Netlify Functions",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed'",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/neon": "^1.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
